# 중단 규칙 재정의 — 담당자 확정안 반영 (Design)

**결정**: 2026-08-11 담당자(해림) 회신으로 중단 규칙 ①·②의 **동작이 확정**됐다. 확정안은
검사지 인쇄 문구와 세 지점에서 어긋나며, 그 경우 **담당자 회신이 우선**한다.

저장소 원칙은 `검사지가 절대 기준`이다. 이 스펙은 그 원칙의 예외를 명시적으로 기록한다 —
검사지 문구가 (a) 없는 과제를 가리키거나(G2의 "낱말 쓰기"), (b) 실제 시행 절차와 다를 때,
검사를 설계·운영하는 담당자의 회신이 최종 기준이 된다.

## 확정 경위

담당자에게 물은 것은 G2 문장 쓰기의 "첫 문장 오반응 시 검사를 중단합니다"가 **실제 검사에서
무엇을 뜻하는가**였다(일시정지 후 재개인가, 건너뛰고 종료인가).

담당자 회신이 질문 범위를 넘어 규칙 ①까지 뒤집었다:

> "아!! 검사 자체를 중단하는 거야! (…) 생각해보니 낱말 해독에서도 의미낱말 1.2.3 연속 틀리면
> **해독 검사도 중단**, 유창성도 실시하지 않아야 하네..!!"

> "1, 2학년 전부 첫 의미낱말 연속 3개 오반응이면 해독 유창성 중단이고 **바로 쓰기로 넘어가면**
> 될 것 같아! 쓰기의 경우 **1학년은 1번 의미단어 오반응이면 바로 중단, 2학년은 1번 쓰기
> 오반응이면 바로 중단!**"

> "중단에 대한 문구는 나중에 수정하더라도.."

## 검사지와 어긋나는 지점

| | 검사지 인쇄 문구 | 담당자 확정 | 현재 구현 |
|---|---|---|---|
| ① 무의미 낱말 해독 | 실시 (제외 대상에 없음) | **미실시** | 실시 |
| ① 문장 읽기유창성 | 미실시 | 미실시 | 미실시 |
| ① 쓰기 과제 | **미실시** | **실시** | 미실시 |
| ② G1 낱말 쓰기 판정 | **의미 낱말 첫 3개 연속** | **1번 하나** | 의미 낱말 첫 3개 |
| ② G2 문장 쓰기 판정 | 첫 문장 | 첫 문장 | 첫 문장 |

**G2 검사지의 "낱말 쓰기" 오기 문제는 소멸한다.** G2에는 낱말 쓰기가 없어 그 문구가 무엇을
가리키는지 물으려 했으나(담당자 회신 대기 질문 2번), 쓰기를 **실시하는** 쪽으로 확정되면서
그 문구가 무엇을 가리키든 동작에 영향이 없어졌다. 해당 질문은 발송하지 않는다.

## 확정 규칙

### ① 낱말 해독 중단 — 의미 낱말 첫 3개 연속 오반응

**미실시**: 무의미 낱말 해독 · 문장 읽기유창성 전체
**실시**: 쓰기 과제 · 검사자 체크리스트
**이동**: 안내 모달 확인 → 쓰기 과제

의미 낱말은 **7문항 전부 채점한다.** 아동은 이미 한 페이지에서 7개를 30초 동안 다 읽었고
(녹음 단위가 페이지다), 채점은 그 녹음에 대한 검사자의 사후 표시다. "중단"은 앞으로 실시할
과제를 실시하지 않는 것이지 이미 읽은 것을 세지 않는 것이 아니다.

### ② 쓰기 중단 — 1번 문항 오반응

양식 구분 없이 **첫 문항 하나**로 판정한다(G1 `ww01` "우비" / G2 `sw01` "집으로 와요").
나머지 문항은 잠기고, 안내 모달 확인 → 검사자 체크리스트.

"오반응"의 정의는 **0점만**이다(`score === 0`) — 담당자 확정. G2 문항 배점이 0·1·2라
1점이 오반응인지 물었고, **1점은 오반응이 아니다**는 회신을 받았다. 기존 구현
(`isWritingWrong`)이 그대로 맞으므로 변경 없다. `lib/survey-flow.ts`의 해당 주석에서
"담당자가 회신하면 이 함수만 바꾸면 된다"를 확정 사실로 갱신한다.

## 화면 동작

두 모달 모두 **안내 후 `goNext()`** 일 뿐이다. `visiblePages()`가 이미 다음 페이지를
정해 놓으므로 별도 이동 로직이 없다. 기존 `components/ConfirmDialog.tsx`를 변경 없이 쓴다.

| | 발화 시점 | 근거 |
|---|---|---|
| ① | 의미 낱말 채점 화면에서 **[다음]을 누를 때** | 7문항 전부 채점해야 하므로, 3개째 X에서 띄우면 4~7번 채점을 막는다 |
| ② | 1번 문항 오반응 **입력 즉시** | 1번 하나로 판정이 끝나 더 받을 입력이 없다 |

`ConfirmDialog`의 취소 버튼을 **오입력 복구 경로로 쓴다**(①: "다시 채점" / ②: "다시 입력").
취소하면 그 화면에 머물러 점수를 고칠 수 있다. 컴포넌트 변경 0건.

②는 즉시 발화이므로 취소 후 같은 값에 다시 뜨지 않도록 **쓰기 화면 로컬 state**로 한 번만
띄운다. 다른 페이지를 거쳐 다시 들어오면 초기화되어 한 번 더 뜬다 — 안내로서 의도된 동작이다.
①은 [다음] 클릭으로만 뜨므로 이 state가 필요 없다.

### 모달 문구 (임시)

```
① 의미 낱말 첫 3개 연속 오반응하여 낱말 해독과 문장 읽기유창성 과제를 중단합니다.
   쓰기 과제로 넘어갑니다.
② (G1) 1번 낱말이 오반응하여 검사를 중단합니다.
   (G2) 첫 문장이 오반응하여 검사를 중단합니다.
```

①은 담당자 제안 원문("…문장 읽기 유창성 과제와 **낱말 쓰기** 과제를 실시하지 않습니다")을
**쓸 수 없다.** 같은 회신에서 쓰기를 실시하는 쪽으로 바뀌었으므로 원문 그대로 쓰면 화면이
거짓을 말한다. ②는 원문 그대로다.

담당자가 "문구는 나중에 수정"이라 했으므로 **두 문구를 한 곳에 모아** 교체가 한 파일에서
끝나게 한다.

⚠️ **이 모달은 아동이 보는 화면에 뜬다.** "첫 3개 연속 오반응" 같은 검사자 언어가 아이 앞에
표시된다. 문구 확정 시 아동 노출을 전제로 재검토할 것.

### 섹션 안내와의 연쇄

① 확인 직후 쓰기 과제 첫 진입이라 `SectionIntro`("문장을 정확하게 쓸 수 있는지 확인해요")가
바로 이어진다. 중단 안내(검사자용) → 과제 안내(아동용) 순서는 담당자 의도("바로 쓰기로 진입")와
맞으므로 **그대로 둔다.** ② → 체크리스트도 같다. 의도된 동작이며 버그가 아니다.

## 채점·진행률

### 미실시의 표기 — 0점이 아니다

무의미 낱말, 문장 읽기유창성, 규칙 ②로 실시하지 않은 쓰기 문항은 모두 **"미실시"** 로
표기한다. 0점으로 채점하지 않는다 — 치르지도 않은 과제에서 낙제한 아동으로 기록된다.

판정 기준은 플래그가 아니라 **응답 부재**다(해당 코드의 marks/점수가 없음). 결과지는 어차피
칸을 그리려면 데이터를 봐야 하므로 이 방식이 짧고, 규칙이 또 바뀌어도 표시가 따라온다.

**표시와 완료 판정은 서로 다른 근거를 쓴다** — 표시는 응답 부재, 완료는 중단 규칙
(`readingCeilingHit`). 의도된 분리다: "이 칸에 무엇을 그리는가"는 데이터가 답할 수 있지만,
"이 과제가 끝난 것인가"는 규칙만이 답할 수 있다(무응답이 미실시인지 미채점인지 구분되지 않는다).

### 낱말 해독 — 완료로 보되 Pass/Fail이 아니라 `중단`으로 표시한다

`complete.wordReading`은 ①이 걸리면 **의미 코드만** 요구한다("더 채점할 것이 없다"는 뜻).
판정을 감추면 중단 세션의 결과지가 빈 문서가 되는데, 정작 중단의 근거가 그 낱말 해독이다.

**그러나 Pass/Fail 배지를 내지 않는다.** `passMark.wordReading = 9`는 `/14` 기준인데
①이 걸린 세션의 실시분은 의미 7문항뿐이다. 기준에 못 미쳐 Fail이 나오고 결론 자체는
임상적으로 옳지만(첫 3개 연속 오반응), **근거 표시가 성립하지 않는다** — 7점짜리 과제에
9점을 요구한 셈이다.

판정 자리에는 `중단` 배지를 낸다. 중단은 Pass/Fail 척도에 밀어넣을 것이 아니라 **그 자체가
결론**이다. 만점 표기는 `/14`를 유지하고(실제 척도), 무의미 소계 칸만 "미실시"로 적는다.

만점을 실시분(`/7`)으로 축소하는 안은 택하지 않았다 — `scoringFor`의 만점이 동적이 되면
채점·PDF 스탬핑까지 번지는데, 얻는 것은 표기 하나다.

구현은 `ScoreResult`에 **`discontinued: Record<TaskKey, boolean>`** 을 추가한다.
`Verdict` 타입은 건드리지 않는다(PDF가 쓰는 타입이며 공식 출력물에 판정은 찍히지 않는다).
배지 선택 우선순위: `discontinued` → `중단` · `!complete` → `채점 전` · 그 외 Pass/Fail.

### 진행률 분모

`FormItems.discontinuedTotals` 정적 필드를 **`expectedTotals(s, writing)` 함수 계산으로**
교체한다. 규칙이 둘로 늘어 조합이 넷이 됐다.

| | rec | write (G1 / G2) |
|---|---|---|
| 중단 없음 | 6 | 10 / 5 |
| ①만 | **1** | 10 / 5 |
| ②만 | 6 | **1** |
| ①+② | **1** | **1** |

②는 **저장된 쓰기 답에서 파생한다** — 컬럼을 추가하지 않는다. `sessionProgress()`가
`writing_answers`·`sentence_scores`를 이미 갖고 있어 1번 문항 점수만 보면 분모가 나온다.

이는 `012_discontinued.sql`이 컬럼을 만든 논리("관리자가 채점을 고치면 파생값이 뒤집힌다")와
어긋난다. 규칙 ②에서는 그 뒤집힘이 **오히려 정확하다**: 관리자가 1번을 0→1로 고쳤다면 그
아동은 중단되지 말았어야 했고 2~5번 데이터는 존재하지 않는다. 분모가 늘어 "1/5 미완료"가
되는 것이 사실이다. 컬럼으로 박으면 잘못 중단된 검사가 영원히 "정상 완료"로 남는다.

### 마이그레이션

**013 — `sessions.discontinued_at`의 `comment on column` 한 줄 갱신.** 컬럼 값의 의미
(=이 세션에 ①이 걸렸다)는 그대로지만, 그 결과로 무엇이 미실시인지가 바뀐다(문장·쓰기 →
무의미·문장). 임상 데이터의 컬럼 설명이 틀린 채 남으면 안 된다. 스키마 변경은 없다.

**소급 대상 0건 확인** — 실 Supabase의 `discontinued_at`은 전체 10개 세션 모두 `null`이다
(2026-08-11 조회). 옛 규칙으로 진행된 중단 세션이 존재하지 않으므로 소급 방어 코드를 넣지
않는다. 만약 배포 전에 생기더라도 `incomplete` 판정은 `recorded < expected`라 과다 계상이
미완료로 잡히지는 않는다.

## 관리자 화면

중단 규칙이 바뀌면 **관리자 결과지가 실시하지 않은 과제를 채점 대상으로 계속 취급한다.**
검사 화면만 고치면 관리자가 없는 녹음을 찾아다니고, 실시하지 않은 문항을 채점할 수 있다.

### 이미 맞는 것 — 변경하지 않는다

- `lib/pdf/stamp-sheet.ts` — `undefined`는 찍지 않는다. 미실시 칸이 저절로 빈다.
- `sheet/ScoreBand.tsx` · 문장 `Subtotal` — `complete=false`면 "채점 전 / 실시하지 않았거나
  채점 전". 문장 읽기유창성 미실시에 이미 정확하다.
- `BadgeLegend.tsx` — "중단 규칙으로 **실시하지 않은** 과제입니다. **0점이 아닙니다**" 설명
  이미 있음.

기존 설계가 "미실시 ≠ 0점"을 이미 관통하고 있어 손댈 곳이 그만큼 줄었다.

### 고쳐야 하는 것

| # | 위치 | 문제 | 처리 |
|---|---|---|---|
| 1 | `sheet/Subtotal.tsx` | ① 세션에서 "무의미 점수 **0** / 7"이 확정값으로 찍힌다. `Subtotal.tsx:22` 주석이 *"이 화면이 가장 경계하는 오독"* 이라 못 박은 바로 그것 | `Cell`에 미실시 상태 추가 → "미실시". 판정 자리는 `중단` 배지 |
| 2 | `sheet/ScoreBand.tsx` | 낱말 해독 카드가 Pass/Fail을 낸다 | `discontinued`면 `중단` |
| 3 | `sheet/PageAudio.tsx` | ① 세션에 빨간 `미녹음` 배지가 5개(무의미 1 + 문장 4). 실시하지 않은 과제인데 "녹음이 빠졌다"고 경고해 관리자가 없는 파일을 찾게 된다 — PR #24가 잡은 경보 피로의 재발 | 회색 `미실시`로 분기 |
| 4 | `sheet/WordScoreRows.tsx` · `sheet/SentenceRows.tsx` | 실시하지 않은 무의미 낱말 O/X와 문장 점수 입력이 열려 있다. 찍으면 총점이 오염된다 | 잠금(읽기 전용) |
| 5 | `AdminDetailView.tsx:124` | KPI 분모가 `f.recordingPages.length`(=6) 고정이라 ① 세션이 "녹음 **1 / 6**"으로 뜬다. `missingCount`는 `expectedTotals`를 써서 따라오는데 화면 숫자만 안 따라온다 — PR #24의 "녹음 18/6"과 같은 버그 계열 | `expectedTotals` 사용 |
| 6 | `sheet/WritingChips.tsx` · `sheet/SentenceWriteRows.tsx` | ②로 실시하지 않은 쓰기 2~5번이 X나 0점으로 보이면 안 된다 | 미입력을 미실시로 그리는지 확인 후 필요 시 분기 |

### 미실시 판정의 단일 근거

여섯 곳이 각자 "이 칸은 미실시인가"를 다시 판정하면 어긋난다. `ResultSheet`가 한 번 계산해
하위 컴포넌트에 내려보낸다 — 판정 로직은 `lib/survey-flow.ts`의 기존 두 함수가 그대로 답한다.

## 검사지 PDF — 중단 이후는 점수를 적지 않는다

**담당자·사용자 확정**: 중단된 지점 이후의 점수는 **아무것도 적지 않는다.** 별도 안내 문구나
표시를 넣지 않는다. 빈칸이 곧 표기다.

검사자 체크리스트는 **평소대로 표기한다** — 중단 여부와 무관하게 늘 실시하는 검사자 관찰
기록이다.

| 칸 | ①만 걸림 | ②도 걸림 |
|---|---|---|
| 의미 낱말 O/X · 의미 점수 `/7` | **찍는다** (중단 이전에 실시·채점됨) | 좌동 |
| 무의미 낱말 O/X · 무의미 점수 `/7` · 총 점수 `/14` | 비움 | 좌동 |
| 문장 읽기유창성 문항별 · 총점 `/36` | 비움 | 좌동 |
| 쓰기 1번 | 찍는다 | 찍는다 |
| 쓰기 2~5번 · 쓰기 소계·총점 | 찍는다 (쓰기는 실시) | 비움 |
| 검사자 체크리스트 | 찍는다 | 찍는다 |

"중단된 것 **이후**"를 시간 순서로 읽는다 — 의미 낱말은 중단이 발생하기 전에 실시된 것이므로
그 소계는 남긴다. 실시하지 않은 것에만 숫자를 남기지 않는다.

### 구현

`stamp-sheet.ts`의 소계·총점 조건이 지금은 `r.complete.*` 하나다. 스펙대로
`complete.wordReading`이 ① 세션에서 **true**가 되므로, 그대로 두면 "무의미 0 / 7 · 총 1 / 14"가
찍힌다. 조건을 `complete && !discontinued`로 좁힌다.

문항별 표시(`stampGrid`·`circleChoice`·문장 점수)는 값이 `undefined`면 이미 건너뛰므로
변경 없다. 실시하지 않은 문항은 값 자체가 없다.

### 화면과 표기가 갈리는 이유

관리자 결과지 화면은 `중단` 배지와 함께 총점 `/14`를 보여주고, 검사지 PDF는 그 칸을 비운다.
모순이 아니라 역할 차이다 — 화면은 **채점 작업대**라 상태를 다 드러내야 하고(옆에 배지가
붙어 오독을 막는다), PDF는 **학교로 나가는 공식 문서**라 실시하지 않은 것에 숫자를 남기면
그 숫자만 홀로 읽힌다.

## 변경 지점

| 파일 | 변경 |
|---|---|
| `lib/survey-flow.ts` | `visiblePages()` 조건 교체(무의미 낱말 페이지도 제외) · `writingCeilingRule()` **삭제**(n=1 통일로 양식 분기 소멸) |
| `lib/scoring.ts` | `complete.wordReading` — ① 시 의미 코드만 요구 · `ScoreResult.discontinued` 추가 |
| `lib/items.ts` | `discontinuedTotals` 정적 필드 제거 |
| `lib/adminStats.ts` | `expectedTotals()` — 위 4조합 |
| `app/survey/page.tsx` | 모달 2개 + ② 로컬 dismiss state |
| `components/admin/ResultSheet.tsx` | 미실시 판정을 한 번 계산해 하위로 전달 |
| `components/admin/sheet/*.tsx` | 위 관리자 화면 표 1·2·3·4·6 |
| `components/admin/AdminDetailView.tsx` | KPI 분모를 `expectedTotals`로 |
| `lib/pdf/stamp-sheet.ts` | 소계·총점 조건을 `complete && !discontinued`로 |
| `supabase/migrations/013_*.sql` | 컬럼 주석 갱신 |

`readingCeilingHit()`·`writingCeilingHit()`는 **이미 존재하며 그대로 쓴다.** 둘을 묶는 통합
판정 함수는 만들지 않는다 — 소비처가 각각 하나씩만 쓰므로 묶어서 벌 것이 없다.

`app/api/sessions/submit/route.ts`의 `discontinued` 판정(규칙 ①)은 변경 없다.

## 하지 않는 것

- **Pass 기준 확정** — `passMark`는 임시값을 유지하고 `PROVISIONAL_CRITERIA = true`도 그대로다.
  사용자 확정: **"임시로 일단 해두고 나중에 바꾼다."** 기준표가 오면 양식의 숫자만 교체되며
  이미 채점한 세션도 저장된 점수로 다시 계산된다. 이 작업과 독립이다.
- **`isWritingWrong` 변경** — 1점은 오반응이 아니라는 회신을 받았고 현행 구현이 그대로 맞다.
- **모달 문구 확정** — 담당자가 "나중에 수정"이라 했다. 교체가 쉽도록 한 곳에 모으기만 한다.
- **3~6학년 대응** — 검사지 미수령. G1 폴백 그대로.
- **PDF의 중단 안내 문구** — 넣지 않기로 확정. 빈칸이 곧 표기다(위 PDF 절).

## 테스트

기존 테스트 중 **기대값이 바뀌는 것**:

- `tests/survey-flow.test.ts` — `visiblePages` 중단 케이스(무의미 페이지 제외, 쓰기 페이지
  포함), `requiredWritingCodes` G1 케이스(3개 → 1개)
- `tests/items.test.ts:209` — `discontinuedTotals` 제거에 따른 정리
- `tests/adminStats.test.ts:321` — `expectedTotals` 4조합

새로 덮을 것:

- ① 시 무의미 낱말 페이지가 빠지고 쓰기 페이지가 남는다 (G1·G2 각각)
- ② 시 1번 문항만 요구된다 (G1 `ww01` / G2 `sw01`)
- ①+② 동시 성립 시 분모가 `{ rec: 1, write: 1 }`
- ① 시 `complete.wordReading === true` **이면서** `discontinued.wordReading === true` —
  Pass/Fail이 아니라 `중단`으로 갈린다
- ① 시 `discontinued.sentenceReading === true` (문장 읽기유창성도 미실시)
- ② 시 `discontinued.writing === true`, 1번 문항만 채점 대상
- 중단이 없는 세션은 `discontinued`가 전부 false — 기존 Pass/Fail 경로가 그대로다
- G1 "모두 아니오" 일괄 클릭이 `ww01`만 남긴다 (기존 `onSetAll` 로직이 n=1에서도 유효한지)
- **PDF** — ① 세션에서 의미 점수 `/7`은 찍히고 무의미 `/7`·총 `/14`·문장 총점은 **찍히지 않는다**
- **PDF** — ② 세션에서 쓰기 소계·총점이 찍히지 않는다
- **PDF** — 중단 여부와 무관하게 검사자 체크리스트는 늘 찍힌다

## 검증

```bash
npx tsc --noEmit && npm run lint && TZ=UTC npx vitest run && npm run build
```
